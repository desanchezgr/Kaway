# Modelo 3D del poncho

Copia tu archivo GLB con este nombre exacto:

`poncho.glb`

La ruta final debe ser:

`public/models/poncho.glb`

El personalizador de Kaway lo cargara automaticamente en la seccion Atelier.
